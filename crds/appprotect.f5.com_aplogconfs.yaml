apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: aplogconfs.appprotect.f5.com
spec:
  conversion:
    strategy: None
  group: appprotect.f5.com
  names:
    kind: APLogConf
    listKind: APLogConfList
    plural: aplogconfs
    singular: aplogconf
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.bundle.state
      name: STATUS
      type: string
    - jsonPath: .status.bundle.location
      name: LOCATION
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: APLogConf is the Schema for the aplogconfs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APLogConfSpec defines the desired state of APLogConf
            properties:
              content:
                properties:
                  escaping_characters:
                    items:
                      properties:
                        from:
                          type: string
                        to:
                          type: string
                      type: object
                    type: array
                  format:
                    enum:
                    - splunk
                    - arcsight
                    - default
                    - user-defined
                    - grpc
                    type: string
                  format_string:
                    type: string
                  list_delimiter:
                    type: string
                  list_prefix:
                    type: string
                  list_suffix:
                    type: string
                  max_message_size:
                    pattern: ^([1-9]|[1-5][0-9]|6[0-4])k$
                    type: string
                  max_request_size:
                    pattern: ^([1-9]|[1-9][0-9]|[1-9][0-9]{2}|[1-9][0-9]{3}|10[0-2][0-9][0-9]|[1-9]k|10k|any)$
                    type: string
                type: object
              filter:
                properties:
                  request_type:
                    enum:
                    - all
                    - illegal
                    - blocked
                    type: string
                type: object
            type: object
          status:
            description: APLogConfStatus defines the observed state of LogConf
            properties:
              bundle:
                description: Bundle holds the “ready/pending/invalid” bundle info
                properties:
                  compilerVersion:
                    description: CompilerVersion is the version of the compiler used
                      to build this bundle.
                    type: string
                  location:
                    description: Location is the path/URL where the compiled bundle
                      is stored; only set when State == “ready”
                    type: string
                  sha256:
                    description: Sha256 is the SHA256 hash of the bundle file
                    type: string
                  state:
                    description: State is the current bundle state (pending, processing,
                      ready, invalid)
                    enum:
                    - pending
                    - processing
                    - ready
                    - invalid
                    type: string
                required:
                - state
                type: object
              inProgressBundleLocation:
                description: |-
                  InProgressBundleLocation stores the S3 location of the bundle currently
                  being compiled. It is set atomically with the two-phase claim (before
                  the compiler gRPC call) and cleared on success (once Bundle.Location
                  takes its place) or failure. Housekeeping treats it as a protected
                  reference so the shared bundles/ cleanup pass cannot delete an in-flight
                  bundle whose LastModified is older than the current round's roundStart.
                type: string
              inProgressGeneration:
                description: |-
                  InProgressGeneration records the metadata.generation for which a
                  compilation job is currently pending or processing
                format: int64
                type: integer
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent metadata.generation for which the
                  controller successfully processed this APLogConf resource.
                  This field tracks the Kubernetes metadata.generation to determine if
                  the resource needs reprocessing when the spec changes.
                format: int64
                type: integer
              previousBundleLocation:
                description: |-
                  PreviousBundleLocation stores the S3 location of the previous (N-1) bundle.
                  When a new bundle is compiled, the old bundle is NOT deleted immediately
                  because traffic nodes may still be fetching it. Instead, the old location
                  is saved here. On the NEXT successful compilation, the bundle at this
                  location (now N-2) is deleted, and the current bundle location takes its place.
                type: string
              processing:
                description: Processing holds the compiler/validation metadata
                properties:
                  datetime:
                    description: Datetime is when the last compile/validation occurred.
                    format: date-time
                    type: string
                  errors:
                    description: Errors holds any validation or compile errors (only
                      if State == “invalid”)
                    items:
                      type: string
                    type: array
                  isCompiled:
                    description: IsCompiled is true if we compiled the bundle; false
                      means we accepted a pre-compiled one.
                    type: boolean
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: APLogConf is the Schema for the aplogconfs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APLogConfSpec defines the desired state of APLogConf
            properties:
              content:
                properties:
                  escaping_characters:
                    items:
                      properties:
                        from:
                          type: string
                        to:
                          type: string
                      type: object
                    type: array
                  format:
                    enum:
                    - splunk
                    - arcsight
                    - default
                    - user-defined
                    - grpc
                    type: string
                  format_string:
                    type: string
                  list_delimiter:
                    type: string
                  list_prefix:
                    type: string
                  list_suffix:
                    type: string
                  max_message_size:
                    pattern: ^([1-9]|[1-5][0-9]|6[0-4])k$
                    type: string
                  max_request_size:
                    pattern: ^([1-9]|[1-9][0-9]|[1-9][0-9]{2}|[1-9][0-9]{3}|10[0-2][0-9][0-9]|[1-9]k|10k|any)$
                    type: string
                type: object
              filter:
                properties:
                  request_type:
                    enum:
                    - all
                    - illegal
                    - blocked
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: false
