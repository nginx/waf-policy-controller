apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: apusersigs.appprotect.f5.com
spec:
  conversion:
    strategy: None
  group: appprotect.f5.com
  names:
    kind: APUserSig
    listKind: APUserSigList
    plural: apusersigs
    singular: apusersig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.installationState
      name: STATUS
      type: string
    - jsonPath: .spec.tag
      name: TAG
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: APUserSig is the Schema for the apusersigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APUserSigSpec defines the desired state of APUserSig
            properties:
              properties:
                type: string
              signatures:
                items:
                  properties:
                    accuracy:
                      enum:
                      - high
                      - medium
                      - low
                      type: string
                    attackType:
                      properties:
                        name:
                          type: string
                      type: object
                    description:
                      type: string
                    name:
                      type: string
                    references:
                      properties:
                        type:
                          enum:
                          - bugtraq
                          - cve
                          - nessus
                          - url
                          type: string
                        value:
                          type: string
                      type: object
                    risk:
                      enum:
                      - high
                      - medium
                      - low
                      type: string
                    rule:
                      type: string
                    signatureType:
                      enum:
                      - request
                      - response
                      type: string
                    systems:
                      items:
                        properties:
                          name:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              softwareVersion:
                type: string
              tag:
                type: string
            type: object
          status:
            description: APUsersigStatus defines the observed state of Usersig
            properties:
              installationState:
                description: 'InstallationState represents the installation status:
                  success, installing, or failure'
                enum:
                - success
                - installing
                - failure
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent metadata.generation for which the
                  controller successfully processed this APUserSig resource.
                  This field tracks the Kubernetes metadata.generation to determine if
                  the resource needs reprocessing when the spec changes.
                format: int64
                type: integer
              policyUpdateState:
                description: 'PolicyUpdateState represents the state of embedding
                  new signature updates in policies: complete or ongoing'
                enum:
                - complete
                - ongoing
                type: string
              processing:
                description: Processing holds upload/validation metadata.
                properties:
                  datetime:
                    description: Datetime is when the last compile/validation occurred.
                    format: date-time
                    type: string
                  errors:
                    description: Errors holds any validation or compile errors (only
                      if State == “invalid”)
                    items:
                      type: string
                    type: array
                  isCompiled:
                    description: IsCompiled is true if we compiled the bundle; false
                      means we accepted a pre-compiled one.
                    type: boolean
                type: object
            required:
            - installationState
            - policyUpdateState
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: APUserSig is the Schema for the apusersigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APUserSigSpec defines the desired state of APUserSig
            properties:
              properties:
                type: string
              signatures:
                items:
                  properties:
                    accuracy:
                      enum:
                      - high
                      - medium
                      - low
                      type: string
                    attackType:
                      properties:
                        name:
                          type: string
                      type: object
                    description:
                      type: string
                    name:
                      type: string
                    references:
                      properties:
                        type:
                          enum:
                          - bugtraq
                          - cve
                          - nessus
                          - url
                          type: string
                        value:
                          type: string
                      type: object
                    risk:
                      enum:
                      - high
                      - medium
                      - low
                      type: string
                    rule:
                      type: string
                    signatureType:
                      enum:
                      - request
                      - response
                      type: string
                    systems:
                      items:
                        properties:
                          name:
                            type: string
                        type: object
                      type: array
                  type: object
                type: array
              softwareVersion:
                type: string
              tag:
                type: string
            type: object
        type: object
    served: true
    storage: false
